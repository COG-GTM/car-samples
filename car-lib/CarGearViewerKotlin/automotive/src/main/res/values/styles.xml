<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark "MBUX" theme layered on the car's default theme. -->
    <style name="Theme.MBUX" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/mb_black</item>
        <item name="android:colorBackground">@color/mb_black</item>
        <item name="android:textColorPrimary">@color/mb_silver</item>
    </style>
</resources>
